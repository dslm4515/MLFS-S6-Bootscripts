#!/bin/execlineb -P

# Load Kernel Modules (from config)

fdmove -c 2 1

if { s6-echo --  "[     modules    ] Feature not available yet: Loading kernel modules via config file" }
s6-true

# Will implement later.
