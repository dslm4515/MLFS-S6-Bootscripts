#!/bin/execlineb -P

# Set time & date from Hardware Clock
# Will improve later for customization

fdmove -c 2 1

#hwclock --hctosys --localtime --noadjfile

if { s6-echo -- "[    hardclock   ] Feature not available yet: System clock was not set." }
s6-true
