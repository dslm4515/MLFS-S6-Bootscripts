#!/bin/execlineb -P

# Mount Root Filesystem Read-Write

fdmove -c 2 1

if { s6-echo -- [ ..FSrw.. ] Remounting RootFS as Read-Write... }
if { s6-mount -o remount,rw / / }
if { s6-echo -- [ ..FSrw.. ] RootFS is Read-Write. }

