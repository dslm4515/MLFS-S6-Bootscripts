#!/bin/execlineb -P

# Mount Root Filesystem Read-Only

fdmove -c 2 1

if { s6-echo -- [   FSro   ] Remounting RootFS as Read-Only... }
if { s6-mount -o remount,ro  / / }
if { s6-echo -- [   FSro   ] RootFS is Read-Only. }

